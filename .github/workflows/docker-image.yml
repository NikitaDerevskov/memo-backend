name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up -d

  lint:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Run lint
        run: docker-compose exec memo-backend_memo-backend_1 npm i; docker-compose exec memo-backend_memo-backend_1 npm run lint

